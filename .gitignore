# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/

# Jupyter
.ipynb_checkpoints/
*.nbconvert.ipynb

# OS / IDE (macOS Finder; ignore in every folder)
.DS_Store
**/.DS_Store
.cursor/

# Secrets (never commit)
.env
*.pem
HF_TOKEN

# Large / local caches (datasets download at runtime)
.cache/

# Local weight dumps (optional; Hub download preferred)
homework 1/mlx_bert/weights/*.npz

# Machine-specific training logs (keep locally; do not commit)
homework 1/part2_mlx_run*.log

# HW2 — large CSVs exceed GitHub file size limits; keep local copies in DataFiles/
homework 2/DataFiles/UNSW-NB15_1.csv
homework 2/DataFiles/Wednesday-workingHours.pcap_ISCX.csv
